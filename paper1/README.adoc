= Paper 1 build notes

This directory contains the first-submission manuscript only.

It does not claim an optimal VAT rate or a structural macro effect.
The paper asks what can be identified about Japanese personal income-tax
marginal incidence using public aggregate data when filer microdata are not
available for redistribution/reproduction.

== Build

[source,shell]
----
python paper1/scripts/generate_stage1_attributes.py --check
python paper1/scripts/generate_frontier_table.py --check
python paper1/scripts/validate_claim_registry.py
asciidoctor paper1/index.adoc
----

== Authority order

. Reproduced Git results and official public-source inputs.
. Explicit partial-identification propositions.
. Recovered V6 prior-run results, labelled as recovery-required.
. Sensitivity-only constructions.
. NOT_READY quantities, which cannot enter headline conclusions.
