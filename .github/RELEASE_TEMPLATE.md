# Release research-progress summary

## Scientific progress since the comparison release

Report only decision-relevant changes. Use `N/A` when a KPI is not applicable.

- `voi_gap_advances`:
- `identified_set_width_reduction`:
- `assumption_burden_reduction`:
- `policy_relations_resolved`:
- `independent_review_gates_satisfied`:
- `recovery_only_claims_upgraded_or_retired`:

For the machine-readable snapshot, compare the previous release commit/tag with the release candidate:

```bash
python3 scripts/report_research_progress.py --ref <previous-release-ref>
python3 scripts/report_research_progress.py
```

## Integrity / release readiness

The following prove completeness/reproducibility, not scientific progress on their own.

- Required reproduction targets and CI:
- Manifest file count:
- Raw-source / derived-artifact count:
- Product bundle file count and deterministic hash:
- Scenario / grid / table row counts, if relevant:

## Remaining decision-relevant uncertainty

- Highest-VoI unresolved gap(s):
- Stop rules / external dependencies:
- Scientific review gates still pending:
